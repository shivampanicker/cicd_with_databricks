name: release
on: push

jobs:
  run-databricks-notebook:
   runs-on: ubuntu-latest
   steps:
     - name: Checkout repo
       uses: actions/checkout@v2
     - name: Run a databricks notebook
       uses: databricks/run-notebook@v0
       with:
         local-notebook-path: /Repos/shivam.panicker@databricks.com/cicd_with_databricks/src/main/tests/bronze/test_load_data_into_bronze.py
         databricks-host: ${{ secrets.DATABRICKS_HOST_PRD }}
         databricks-token: ${{ secrets.DATABRICKS_TOKEN_PRD }}
     - existing_cluster_id: 0220-081432-d724g8po